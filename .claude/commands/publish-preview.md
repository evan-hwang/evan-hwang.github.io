---
description: 오늘의 학습 기록 미리보기 (저장하지 않음)
allowed-tools: Bash(python3:*), Bash(pip:*)
---

# 오늘의 학습 기록 미리보기

블로그에 게시하기 전에 미리보기로 확인합니다. 파일은 저장되지 않습니다.

```bash
cd /Users/evanhwang/my-github/project/recoblog && source .venv/bin/activate && python3 -m scripts.publish --date today --dry-run
```
